# ? make è per compilare il file c, uso: make <nomefile> (nome file senza l'estenzione c!)
# definizione del compilatore e dei flag di compilazione
# che vengono usati dalle regole implicite
CC=gcc
CFLAGS=-std=c11 -Wall -O -g
LDLIBS=-lm